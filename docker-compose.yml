version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - AMADEUS_CLIENT_ID=pL66YNSD3oDu5BIuI5hTyRonuEOVRYAj
      - AMADEUS_CLIENT_SECRET=0XQFGqeT7jxy2Gg8
    restart: unless-stopped
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - REACT_APP_API_URL=http://localhost:8080
    restart: unless-stopped
